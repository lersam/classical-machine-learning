# preparation
1. set environment by running ```python3 -m  venv venv```
2. activate virtual environment ```source venv\bin\activate```
3. add python packages by running ```pip install -r requirements.txt```
