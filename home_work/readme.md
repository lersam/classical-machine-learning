# preparation
1. create database: ```CREATE DATABASE movielens_llm
    DEFAULT CHARACTER SET = 'utf8mb4';```
2. set environment by running ```python3 -m  venv venv```
3. activate virtual environment ```source venv\bin\activate```
4. add python packages by running ```pip install -r requirements.txt```
